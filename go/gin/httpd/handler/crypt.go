package handler

/* func Crypt(c *gin.Context) {
	percentage := c.Param("percentage")
	delay := c.Param("delay")
	c.JSON(http.StatusOK, gin.H{
		"message": "Ok",
	})
}

func encrypt(plain) {
	Bcrypt.Base.hash_password(plain, Bcrypt.gen_salt(10, true))
}


func validate(plain, hash) {
	Bcrypt.verify_pass(plain, hash)
} */

